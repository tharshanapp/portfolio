
  # Generate UI Components

  This is a code bundle for Generate UI Components. The original project is available at https://www.figma.com/design/RJDKkpgtpicLoiJLAtkgO1/Generate-UI-Components.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  